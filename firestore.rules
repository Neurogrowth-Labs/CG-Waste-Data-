rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development fallback for demo
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
